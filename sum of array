#include <stdio.h>
int main()
{
    int array[10];
    int sum = 0;
    for (int i = 0; i < 10; i++) 
{
        array[i] = i + 1; 
    }
    for (int i = 0; i < 10; i++) {
        sum += array[i];
    }
    printf("Sum of array elements: %d\n", sum);
    return 0;
}

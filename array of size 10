#include <stdio.h>
int main()
{
    int array[10];
    for (int i = 0; i < 10; i++)
{
        array[i] = i + 1;
    }
    printf("Array elements are:\n");
    for (int i = 0; i < 10; i++)
{
        printf("array[%d] = %d\n", i, array[i]);
    }
    return 0;
}
